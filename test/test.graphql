
type User @constraints(unique: [{ fields: ["id"] }, { fields: ["name", "city"] }]) {
  id: ID!
  name: String
  city: String
}

type Post {
  id: ID! @hidden
  user: User!
}

enum Position {
  PG
  SG
}
